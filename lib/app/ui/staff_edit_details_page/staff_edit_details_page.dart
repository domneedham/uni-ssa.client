import 'package:flutter/material.dart';

class StaffEditDetailsPage extends StatelessWidget {
  const StaffEditDetailsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Edit Details')),
      body: Text("Edit Details"),
    );
  }
}
